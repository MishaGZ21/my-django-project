# Factory App — быстрый аудит (2025-09-15 16:54)

## Общее
- Django проект обнаружен в `/mnt/data/workspace/factory_app`
- Требования: `Django>=5,<6` и `reportlab>=4`
- Настройки читают `.env` (DEBUG, ALLOWED_HOSTS, WhatsApp и т.д.).
- Шаблоны в `core/templates/`, статика — `core/static/` (логотип найден).

## Критичные/явные проблемы
- [x] **Дубликат поля** `Calculation.cost_facades` в `core/models.py` — было две одинаковых строки. Оставлена одна. Это могло ломать миграции/inspections.
- [ ] **Миграции**: в корне есть `MIGRATIONS_NOTE.txt` с указанием на новые поля `lds_color5..10` / `pvc_color5..10`. На проде нужно выполнить `makemigrations`/`migrate`.
- [ ] **Зависимости**: убедиться, что на сервере установлены `Django 5.x` и `reportlab 4.x`.
- [ ] **WhatsApp**: проверить `.env` переменные и наличие одобренных шаблонов в Meta (ключи заданы).

## Про «Расчёт» (фиксация цен)
- В `Calculation` есть `price_snapshot` и логика в `calculation_edit` — кнопка «Загрузить новые цены» присваивает свежий снимок цен. На обычном открытии расчёт НЕ перетягивает цены заново — это **правильное поведение** (зафиксировали).
- Шаблон `core/templates/calculation/edit.html` содержит кнопку `name="action" value="reload_prices"` — всё на месте.

## По ранним баг‑репортам из чата
- Ошибка `customer_phone` — актуальный код использует поле `Order.phone` и ищет по `phone`/`whatsapp_phone`. ОК.
- Дубли навбара — в `base.html` одна навигация. ОК.
- Ошибки ReportLab (`TA_RIGHT`, `mm`, `finders`, `Image`) — соответствующие импорты присутствуют в `core/views.py`. ОК.
- `default_if_none` — в шаблонах не используется. ОК.

## Рекомендации next steps
1. **Выполнить миграции** (особенно после удаления дубликата поля):  
   ```bash
   python manage.py makemigrations core
   python manage.py migrate
   ```
2. **Проверить CRUD «Расчёта»** на новых заказах: создать заказ → заполнить Лист закупа → открыть «Расчёт» → убедиться, что снапшот цен создаётся один раз и пересоздаётся только по кнопке.
3. **Регрессионно проверить PDF‑ы**: `purchase_sheet_pdf`, `purchase_pdf`, `payment_receipt_pdf` — что логотип тянется, стили корректны.
4. **Тест «Заказы / поиск»**: по номеру, по телефону, по WhatsApp.
5. **WhatsApp**: через `/whatsapp/` убедиться, что dashboard отображает настройки, а тестовая отправка `.../whatsapp/test-send/` отрабатывает.

---

_Этот файл сгенерирован автоматически._
